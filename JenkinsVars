pipeline {
    agent any
    stages {
        stage('Variable_Demo') {
            steps {
                echo "Job ${env.JOB_NAME} is running on ${env.JENKINS_URL} and having build no: ${env.BULID_ID}"
            }
        }
    }
}
