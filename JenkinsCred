pipeline {
    agent any
    environment{
     JENKINS_DEMO=credentials('DEMO_SECRET')
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'echo "My scret cred is : ${JENKINS_DEMO}"'
            }
        }
    }
}
